//TJWC can't remember what sort of grouping this is.
// I think you mean buttonH,buttonW line is one instance of grouping?
// this code needs a lot of simplification to be a clear example
// you can delete the bits of real code you don't need which are distracting
// in SheetDisplay.fs
// look at  let {BoundingBox.TopLeft = {X=fX; Y=fY}; H=fH; W=fW} = model.ScalingBox.Value.ScalingBoxBound
//          let buttonH, buttonW = (fH+100.), (fW+100.)

module Grouping =

    let extractedCode = 
                let {BoundingBox.TopLeft = {X=fX; Y=fY}; H=fH; W=fW} = model.ScalingBox.Value.ScalingBoxBound //the syntax here is confusing
                let buttonH, buttonW = (fH+100.), (fW+100.) // grouping
                drawAnnotation model.ScalingBox.Value.RotateDeg270Button buttonH buttonW
                @ drawAnnotation model.ScalingBox.Value.RotateDeg90Button buttonH buttonW
                @ drawAnnotation model.ScalingBox.Value.ScaleButton buttonH buttonW